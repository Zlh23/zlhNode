---
description: 每次修改 Blender 插件代码后必须递增 bl_info 版本号
globs: web/blender_addon/**/*.py
---

# Blender 插件版本号规则 — 硬性必须

## 铁律

**在每一次对话中，只要你修改了 `web/blender_addon/` 目录下的任何 `.py` 文件（包括创建新文件、修改已有文件、删除文件），都必须在同一次对话中递增 `bl_info["version"]`。**

这条规则没有例外，即使修改的只是注释、类型标注或空行。

## 怎么做

每次修改代码后，找到 `web/blender_addon/zlh_dataset_upload/__init__.py` 中的：

```python
bl_info = {
    ...
    "version": (0, 0, NNN),
    ...
}
```

将 `NNN` 加 1。

## 版本号策略

- **主版本 (x.0.0)**：重大架构变更、不兼容的 API 改动
- **次版本 (0.x.0)**：新增功能、核心逻辑变更
- **修订版本 (0.0.x)**：Bug 修复、小优化、文字修改（其他情况默认走这个）

## 为什么

Blender 插件的「检查更新」功能通过比较 GitHub Release tag 与 `bl_info["version"]` 来判断是否有新版本。如果不更新版本号，用户将无法收到更新推送。

## 发布规则

1. **版本递增**：每次修改功能代码后**必须递增版本号**。不允许在同一个 tag 下反复替换 zip。
2. **永远不要删除远程已有的 tag/release**。如果不小心 push 了错误的 tag，在当前版本号基础上递增到下一个版本，而不是删掉重来。
3. **同版本内的小修小补**：如果改动极小（如修一个注释拼写），可以只 push commit 不发布新 release，等下次功能更新时一起发。

## 发布前确认清单

- [ ] `bl_info["version"]` 已递增
- [ ] 确认是**新版本号**（不是已存在的 tag）
- [ ] 打包 zip 并上传到 Release 的 assets
